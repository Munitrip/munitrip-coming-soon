{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    // 1. Eliminamos baseUrl
    "paths": {
      "@/*": ["./src/*"] // 2. Agregamos el prefijo ./src/ aquí
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", ".astro/types.d.ts", "**/*.d.ts"],
  "exclude": ["node_modules", "dist"]
}
